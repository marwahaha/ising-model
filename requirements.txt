# Tested with Python 3.10. Plotly's JS bundle is fetched from the CDN into
# vendor/ on first plot.py run, so only the Python package is needed here.
numpy>=1.24
networkx>=3.0
plotly>=5.0
matplotlib>=3.7
